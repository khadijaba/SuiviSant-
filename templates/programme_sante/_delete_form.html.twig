<form method="post" action="{{ path('app_programme_sante_delete', {'id': programme_sante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programme_sante.id) }}">
    <button class="btn">Delete</button>
</form>
