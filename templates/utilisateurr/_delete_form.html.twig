<form method="post" action="{{ path('app_utilisateurr_delete', {'id': utilisateurr.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateurr.id) }}">
    <button class="btn">Delete</button>
</form>
