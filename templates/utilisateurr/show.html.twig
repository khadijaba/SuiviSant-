{% extends 'base.html.twig' %}

{% block title %}Utilisateurr{% endblock %}

{% block body %}
    <h1>Utilisateurr</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateurr.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateurr.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ utilisateurr.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateurr.email }}</td>
            </tr>
            <tr>
                <th>Date_naissance</th>
                <td>{{ utilisateurr.dateNaissance ? utilisateurr.dateNaissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ utilisateurr.sexe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateurr_index') }}">back to list</a>

    <a href="{{ path('app_utilisateurr_edit', {'id': utilisateurr.id}) }}">edit</a>

    {{ include('utilisateurr/_delete_form.html.twig') }}
{% endblock %}
