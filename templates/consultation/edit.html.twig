{% extends 'base.html.twig' %}

{% block title %}Modifier la Consultation{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="h3 mb-0">Modifier la Consultation</h1>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-warning">Enregistrer</button>
                <a href="{{ path('app_consultation_index') }}" class="btn btn-secondary">Annuler</a>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %} 