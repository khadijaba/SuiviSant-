<form method="post" action="{{ path('app_programme_delete', {'id': programme.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce programme?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programme.id) }}">
    <button class="btn btn-danger"><i class="bi bi-trash"></i> Supprimer</button>
</form> 